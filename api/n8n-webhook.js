export default function handler(req,res){if(req.method==='GET')return res.status(200).json({service:'KENDALIKU n8n webhook',events:['whatsapp.message.received','payment.proof.received','payment.verified','order.approved','followup.due'],status:'ready'});if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const body=req.body||{};return res.status(202).json({accepted:true,event:body.event||'unknown',message:'Webhook diterima. Hubungkan workflow n8n untuk memproses payload.'})}
